---
import Heading from "./ui/Heading.astro";
---

<section class="wrapper">
  <Heading
    as="h1"
    class="name gradient-text gradient-color outline outline-color"
  >
    Hritesh Sonawane
  </Heading>
  <p class="subtext">Security Engineer. OSS Wizard. Lifelong Student.</p>
  <p class="hello">
    Hi, this is Hritesh. I'm documenting my learning notes in this blog. I have a keen interest in 
    <a class="animated">AI</a>,
    and a proven track record in 
    <a class="animated">cybersecurity</a>.<div class=""></div>
    Based on the number of grammar mistakes in my posts, you can tell how much 
    <a class="animated" href={"https://chat.openai.com"} target="_blank">ChatGPT</a> 
    is involved 😉.
  </p>
  <!-- <p class="cli-command">
    <code>Here's a snapshot of my professional and academic accomplishments:</code>
  </p> -->
  <hr />
</section>

<style>
  .wrapper {
    padding-top: 1rem;
    /* padding-bottom: 1rem; */
  }

  .outline {
    -webkit-text-fill-color: transparent;
    -webkit-text-stroke: 0.075rem var(--outline-color);
  }

  .gradient-color {
    --gradient-text-from: #5433ff;
    --gradient-text-via: #20bdff;
    --gradient-text-to: #a5fecb;
  }

  .outline-color {
    --outline-color: var(--text-heading);
  }

  @media (prefers-color-scheme: dark) {
    .gradient-color {
      --gradient-text-from: #a5fecb;
      --gradient-text-via: #20bdff;
      --gradient-text-to: #a5fecb;
    }
    .outline-color {
      --outline-color: transparent;
    }
  }

  .name {
    font-size: 3rem;
    margin: 0;
    letter-spacing: -2px;
    line-height: 3.5rem;
  }

  .subtext {
    color: var(--text-heading);
    font-size: 1.25rem;
    letter-spacing: -0.51px;
    line-height: 2rem;
    margin-top: 0.5rem;
  }

  .cli-command {
    text-align: center;
    white-space: nowrap;
    overflow: auto;
  }

  @media (min-width: 640px) {
    .name {
      font-size: 4rem;
      line-height: 4.75rem;
    }
    .subtext {
      font-size: 1.5rem;
      letter-spacing: -0.51px;
      line-height: 2rem;
      margin-top: 0.5rem;
    }
  }
</style>
